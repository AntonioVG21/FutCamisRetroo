# Firebase Configuration
# You only need to set your Firebase project ID
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id

# Note: Other Firebase configuration values are now directly set in src/lib/firebase.ts
# No need to set these environment variables anymore

# Old Supabase variables (no longer used)
# VITE_SUPABASE_URL=your-supabase-url
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key